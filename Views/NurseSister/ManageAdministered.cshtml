@model ONT_3rdyear_Project.Models.PatientMedicationScript

@{
    ViewData["Title"] = "Medication Already Administered";
}
<div class="container mt-4">
<h2 class="mt-4">Medication Already Administered</h2>

@if (TempData["Message"] != null)
{
    <div class="alert alert-info">
        @TempData["Message"]
    </div>
}

<table class="table table-bordered mt-3">
    <tr>
        <th>Patient Name</th>
        <td>@Model.Patient?.FirstName @Model.Patient?.LastName</td>
    </tr>
    <tr>
        <th>Medication</th>
        <td>@Model.Medication?.Name</td>
    </tr>
    <tr>
        <th>Visit Date</th>
        <td>@Model.VisitSchedule?.VisitDate.ToString("yyyy-MM-dd")</td>
    </tr>
    <tr>
        <th>Dosage</th>
        <td>@Model.Dosage</td>
    </tr>
    <tr>
        <th>Administered By</th>
        <td>@Model.AdministeredBy?.FullName</td>
    </tr>

</table>

<div class="mt-3">
    @* <a asp-action="EditAdministered" asp-route-id="@Model.Id" class="btn btn-warning me-2">Edit</a>
    <a asp-action="DeleteAdministered" asp-route-id="@Model.Id" class="btn btn-danger me-2">Delete</a>
    <a asp-action="Administered" class="btn btn-secondary">Back to List</a> *@
    @* @{
        int userId = 0;
        int.TryParse(ViewBag.CurrentUserId?.ToString(), out userId);
    }

    @if (User.IsInRole("NursingSister") || Model.ApplicationUserID == userId)
    { *@
        <a asp-action="EditAdministered" asp-route-id="@Model.Id" class="btn btn-warning">Edit</a>
    @*     <a asp-action="DeleteAdministered" asp-route-id="@Model.Id" class="btn btn-danger">Delete</a>
    }
    else
    {
        <span class="text-muted">You cannot modify this medication.</span>
    } *@

</div>
</div>

@* <!-- Success Modal -->
<div class="modal fade" id="successModal" tabindex="-1" role="dialog" aria-labelledby="successModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content border-success">
            <div class="modal-header bg-success text-white">
                <h5 class="modal-title" id="successModalLabel">Success</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                @TempData["SuccessMessage"]
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-success" data-bs-dismiss="modal">OK</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            var hasMessage = '@TempData["SuccessMessage"]';
            if (hasMessage) {
                var successModal = new bootstrap.Modal(document.getElementById('successModal'));
                successModal.show();
            }
        });
    </script>
}
 *@
